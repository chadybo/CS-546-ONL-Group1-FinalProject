<div class="page-header">
  <h1>Browse Complaints</h1>
  <p>Explore noise complaints from NYC 311 and community submissions.</p>
</div>

<div class="filter-bar">
  <form method="GET" action="/complaints/browse" style="display:flex;gap:10px;flex-wrap:wrap;align-items:flex-end;width:100%" data-validate-form novalidate>
    <div class="alert alert-error form-error-summary filter-error-summary" role="alert" aria-live="assertive" hidden></div>
    <div>
      <label style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">Borough</label>
      <select name="borough">
        <option value="">All boroughs</option>
        <option value="Manhattan" {{#if isManhattan}}selected{{/if}}>Manhattan</option>
        <option value="Brooklyn" {{#if isBrooklyn}}selected{{/if}}>Brooklyn</option>
        <option value="Queens" {{#if isQueens}}selected{{/if}}>Queens</option>
        <option value="Bronx" {{#if isBronx}}selected{{/if}}>Bronx</option>
        <option value="Staten Island" {{#if isStatenIsland}}selected{{/if}}>Staten Island</option>
      </select>
    </div>
    <div>
      <label style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">Type</label>
      <select name="complaintType">
        <option value="">All types</option>
        <option value="Loud Music/Party" {{#if isLoudMusic}}selected{{/if}}>Loud Music/Party</option>
        <option value="Construction" {{#if isConstruction}}selected{{/if}}>Construction</option>
        <option value="Barking Dog" {{#if isBarkingDog}}selected{{/if}}>Barking Dog</option>
        <option value="Vehicle Idling" {{#if isVehicleIdling}}selected{{/if}}>Vehicle Idling</option>
        <option value="Loud Talking" {{#if isLoudTalking}}selected{{/if}}>Loud Talking</option>
        <option value="Other" {{#if isOther}}selected{{/if}}>Other</option>
      </select>
    </div>
    <div>
      <label style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">Sort</label>
      <select name="sort">
        <option value="Newest" {{#if isNewest}}selected{{/if}}>Newest</option>
        <option value="Oldest" {{#if isOldest}}selected{{/if}}>Oldest</option>
      </select>
    </div>
    <div>
      <label style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">Status</label>
      <select name="status">
        <option value="Open" {{#if isOpen}}selected{{/if}}>Open</option>
        <option value="Closed" {{#if isClosed}}selected{{/if}}>Closed</option>
      </select>
    </div>
    <div class="form-group filter-date-field">
      <label for="filter-from" style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">From</label>
      <input id="filter-from" type="date" name="from" value="{{from}}" data-validate data-date-start aria-describedby="filter-from-error" />
      <span id="filter-from-error" class="field-error" aria-live="polite"></span>
    </div>
    <div class="form-group filter-date-field">
      <label for="filter-to" style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">To</label>
      <input id="filter-to" type="date" name="to" value="{{to}}" data-validate data-date-end aria-describedby="filter-to-error" />
      <span id="filter-to-error" class="field-error" aria-live="polite"></span>
    </div>
    <div>
      <label style="display:block;font-size:12px;font-weight:600;color:#495057;margin-bottom:4px">Search</label>
      <input type="text" name="search" value="{{search}}" maxlength="100" data-validate placeholder="Address, type, keyword..." style="min-width:200px" />
    </div>
    <input type="hidden" name="page" value="1" />
    <button type="submit" class="btn btn-primary">Filter</button>
  </form>
</div>

{{#if complaints}}
  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th>Type</th>
          <th>Address</th>
          <th>Borough</th>
          <th>Date</th>
          <th>Source</th>
          <th>Status</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        {{#each complaints}}
          <tr>
            <td style="font-weight:500">{{this.complaintType}}</td>
            <td style="color:#495057">{{this.incidentAddress}}</td>
            <td>{{this.borough}}</td>
            <td style="color:#adb5bd;font-size:13px">{{this.createdDate}}</td>
            <td>
              {{#if (eq this.source 'user')}}
                <span class="badge badge-user">User</span>
              {{else}}
                <span class="badge badge-311">311</span>
              {{/if}}
            </td>
            <td>
              {{#if (eq this.status 'open')}}
                <span class="badge badge-open">Open</span>
              {{else if (eq this.status 'resolved')}}
                <span class="badge badge-resolved">Resolved</span>
              {{else}}
                <span class="badge badge-open">{{this.status}}</span>
              {{/if}}
            </td>
            <td>
              <a href="/complaints/{{this._id}}" class="btn btn-outline btn-sm">View</a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <div class="pagination">
    <span style="color:#adb5bd;font-size:13px">Page {{currPage}} of {{totPages}}</span>
    <div class="pagination-btns">
      {{#if hPrev}}
        <a href="/complaints/browse?page={{prevPage}}{{queryString}}" class="btn btn-outline btn-sm">Prev</a>
      {{/if}}
      {{#if hNext}}
        <a href="/complaints/browse?page={{nextPage}}{{queryString}}" class="btn btn-outline btn-sm">Next</a>
      {{/if}}
    </div>
  </div>
{{else}}
  <div class="empty-state">
    <p>No complaints found matching your filters.</p>
    <a href="/complaints/browse" class="btn btn-outline">Clear filters</a>
  </div>
{{/if}}
